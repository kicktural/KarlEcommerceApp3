@model List<AssignRoleToUserViewModel>


@* 
<div class="row text text-center">
    <div class="col-md-4 offset-md-4">
        <h2 class="text text-center">Assign User Role</h2>
        <form  asp-action="AssignRoleToUser" asp-route-userId="@ViewBag.userId" method="post">


            @for (int i = 0; i < Model.Count; i++)
            {
                <div class="mb-3 form-check">
                    <input type="checkbox" class="form-check-input" asp-for="@Model[i].Exsit" />
                    <label class="form-check-label">@Model[i].Name</label>
                    <input type="hidden" asp-for="@Model[i].Name" />
                    <input type="hidden" asp-for="@Model[i].Id" />
                </div>
            }

            <div class="form-group mt-3">
                <input type="submit" class="btn btn-primary" value="Role father" />
            </div>


        </form>
        <div class="mt-3">

            <a asp-action="UserList" asp-controller="UserList" class="btn accordion-body">Return to Role List</a>
        </div>
    </div>
</div> *@


<div class="form-check">
    <a href="/Admin/UserList/UserList" class="btn btn-primary">Back</a>
    <form asp-action="AssignRoleToUser" asp-route-userId="@ViewBag.userId" method="post">


        @for (int i = 0; i < Model.Count; i++)
        {
            <div class="mb-3 form-check">
                <input type="checkbox" class="form-check-input" asp-for="@Model[i].Exsit" />
                <label class="form-check-label">@Model[i].Name</label>
                <input type="hidden" asp-for="@Model[i].Name" />
                <input type="hidden" asp-for="@Model[i].Id" />
            </div>
        }

        <div class="form-group mt-3">
            <input type="submit" class="btn btn-primary" value="Role father" />
        </div>


    </form>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
